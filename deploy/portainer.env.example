# Paste into Portainer → Stack → Environment variables (one KEY=value per line).
# Do NOT use "env file" mount — this stack has no env_file: directives.

# --- Required ---
POSTGRES_PASSWORD=change-me
SERVER_API_SECRET_KEY=change-me-to-a-long-random-secret
CULTUR_DOMAIN=cultur.eqnox.com
CULTUR_API_DOMAIN=api.cultur.eqnox.com
MUSICBRAINZ_CONTACT=you@example.com

# --- Deploy / tunnel ---
CULTUR_HTTP_PORT=8080
# Absolute path to deploy/ on the server (Portainer Git stack). Example:
# CULTUR_DEPLOY_DIR=/data/compose/70/deploy
POSTGRES_DB=cultur
POSTGRES_USER=cultur

# --- API keys (optional but recommended) ---
TMDB_API=
TMDB_LANG=en-US
OMDB_API_KEY=
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=
HARDCOVER_API_TOKEN=
BGG_API_TOKEN=
FANART_API_KEY=
LASTFM_API_KEY=
